"""Prompt templates.

Each prompt is a versioned `.txt` (or `.j2`) file in this package. The
filename is the canonical identifier — bump the suffix (`v1`, `v2`, ...)
when the wording changes so the recording layer can attribute results.
"""
